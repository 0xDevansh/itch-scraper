export type GameInput = {
  authorName: string;
  title: string;
};

export type AuthorInput = {
  name: string;
};

export type Game = {
  title: string;
  description: string;
  screenshots: string[];
};
