name: NPM Auto Test

on: pull_request

jobs:
  test_pull_request:
    runs-on: ubuntu_latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with: 
          node-version: 12
      - run: npm ci
      - run: npm test
      - run: npm run compile